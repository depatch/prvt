import React from 'react';
import styles from './ClubList.module.css';

const ClubList: React.FC = () => {
  return (
    <div className={styles.clubList}>
      <h2>Clubs</h2>
      {/* Add club list items here */}
    </div>
  );
};

export default ClubList;